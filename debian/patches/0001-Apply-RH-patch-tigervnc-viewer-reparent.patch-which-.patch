From b18e5f162c48e401345e77091ebe74a75e572f07 Mon Sep 17 00:00:00 2001
From: Joachim Falk <joachim.falk@gmx.de>
Date: Thu, 13 Oct 2011 19:39:56 +0200
Subject: [PATCH 1/7] Apply RH patch tigervnc-viewer-reparent.patch which adds vncviewer embedding support via -Parent option.

---
 unix/tx/TXViewport.cxx       |   15 +++++++++++++++
 unix/tx/TXViewport.h         |   10 ++++++++--
 unix/tx/TXWindow.h           |    5 +++--
 unix/vncviewer/CConn.cxx     |    8 ++++++++
 unix/vncviewer/parameters.h  |    1 +
 unix/vncviewer/vncviewer.cxx |    3 +++
 6 files changed, 38 insertions(+), 4 deletions(-)

diff --git a/unix/tx/TXViewport.cxx b/unix/tx/TXViewport.cxx
index 6064893..0bc8fd1 100644
--- a/unix/tx/TXViewport.cxx
+++ b/unix/tx/TXViewport.cxx
@@ -44,6 +44,21 @@ TXViewport::~TXViewport()
   delete vScrollbar;
 }
 
+void TXViewport::reparent(long embed_window)
+{
+    XReparentWindow(dpy, win(), (Window)embed_window, 0, 0);
+    XSelectInput(dpy, embed_window, child->eventMask);
+    if (!eventHandler)
+       this->setEventHandler(this);
+}
+
+void TXViewport::handleEvent(TXWindow* w, XEvent* ev)
+{
+    if (child && child->eventHandler)
+       ((TXEventHandler *)child->eventHandler)->handleEvent(child, ev);
+}
+
+
 void TXViewport::setChild(TXWindow* child_)
 {
   child = child_;
diff --git a/unix/tx/TXViewport.h b/unix/tx/TXViewport.h
index 823d67a..f1baf51 100644
--- a/unix/tx/TXViewport.h
+++ b/unix/tx/TXViewport.h
@@ -32,8 +32,8 @@
 #include "TXWindow.h"
 #include "TXScrollbar.h"
 
-class TXViewport : public TXWindow, public TXScrollbarCallback,
-                   public rfb::Timer::Callback {
+class TXViewport : public TXWindow, public TXScrollbarCallback, 
+                   public TXEventHandler, public rfb::Timer::Callback {
 public:
   TXViewport(Display* dpy_, int width, int height, TXWindow* parent_=0);
   virtual ~TXViewport();
@@ -59,6 +59,12 @@ public:
   // normally.
   bool bumpScrollEvent(XMotionEvent* ev);
 
+  // reparent the viewport into a new window
+  void reparent(long embed_window);
+
+  // event handler
+  void handleEvent(TXWindow* w, XEvent* ev);
+
 private:
   virtual void resizeNotify();
   virtual void scrollbarPos(int x, int y, TXScrollbar* sb);
diff --git a/unix/tx/TXWindow.h b/unix/tx/TXWindow.h
index 45ee28f..7a2c2da 100644
--- a/unix/tx/TXWindow.h
+++ b/unix/tx/TXWindow.h
@@ -185,6 +185,9 @@ public:
 
   Display* const dpy;
 
+  TXEventHandler* eventHandler;
+  long eventMask;
+
   int xPad, yPad, bevel;
 
 private:
@@ -197,9 +200,7 @@ private:
   TXWindow* parent;
   Window win_;
   int width_, height_;
-  TXEventHandler* eventHandler;
   TXDeleteWindowCallback* dwc;
-  long eventMask;
   XSizeHints sizeHints;
   std::map<Atom,Time> selectionOwnTime;
   std::map<Atom,bool> selectionOwner_;
diff --git a/unix/vncviewer/CConn.cxx b/unix/vncviewer/CConn.cxx
index df6c935..95f291a 100644
--- a/unix/vncviewer/CConn.cxx
+++ b/unix/vncviewer/CConn.cxx
@@ -915,10 +915,18 @@ void CConn::recreateViewport()
 
 void CConn::reconfigureViewport()
 {
+  const char * par = embedParent.getValueStr();
   viewport->setMaxSize(cp.width, cp.height);
   if (fullScreen) {
     viewport->resize(DisplayWidth(dpy,DefaultScreen(dpy)),
                      DisplayHeight(dpy,DefaultScreen(dpy)));
+ } else if (strlen(par) != 0) {
+   XWindowAttributes win_attr;
+   Window w = strtol(par, (char **)NULL, 0);
+
+   XGetWindowAttributes(dpy, w, &win_attr);
+   viewport->reparent(w);
+   viewport->resize(win_attr.width, win_attr.height);
   } else {
     int w = cp.width;
     int h = cp.height;
diff --git a/unix/vncviewer/parameters.h b/unix/vncviewer/parameters.h
index 2661bc7..69f1d7b 100644
--- a/unix/vncviewer/parameters.h
+++ b/unix/vncviewer/parameters.h
@@ -43,6 +43,7 @@ extern rfb::IntParameter compressLevel;
 extern rfb::BoolParameter noJpeg;
 extern rfb::IntParameter qualityLevel;
 extern rfb::BoolParameter passwdInput;
+extern rfb::StringParameter embedParent;
 
 extern char aboutText[];
 extern char* programName;
diff --git a/unix/vncviewer/vncviewer.cxx b/unix/vncviewer/vncviewer.cxx
index facff19..11bb828 100644
--- a/unix/vncviewer/vncviewer.cxx
+++ b/unix/vncviewer/vncviewer.cxx
@@ -111,6 +111,9 @@ StringParameter displayname("display", "The X display", "");
 
 StringParameter via("via", "Gateway to tunnel via", "");
 
+/* Support for reparenting */
+StringParameter embedParent("Parent", "X Window to use as a parent", "");
+
 BoolParameter customCompressLevel("CustomCompressLevel",
 				 "Use custom compression level. "
 				 "Default if CompressLevel is specified.", false);
-- 
1.7.2.5

