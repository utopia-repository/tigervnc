Add __GNU__ define as /usr/include/GL/internal/dri_interface.h needs it. Otherwise include of drm.h will fail!

Index: b/unix/xserver/glx/Makefile.am
===================================================================
--- a/unix/xserver/glx/Makefile.am
+++ b/unix/xserver/glx/Makefile.am
@@ -10,7 +10,8 @@ AM_CFLAGS = \
 	@XLIB_CFLAGS@ \
 	@LIBDRM_CFLAGS@ \
 	@GLX_DEFINES@ \
-	@GLX_ARCH_DEFINES@
+	@GLX_ARCH_DEFINES@ \
+	-D__GNU__
 
 # none yet
 #sdk_HEADERS =
