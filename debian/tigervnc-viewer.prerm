#!/bin/sh
set -e

bindir=/usr/bin
mandir=/usr/share/man

for tiger in xtigervncviewer; do
  notiger=`echo $tiger | sed -e 's/tiger//'`;
  if [ -f $bindir/$tiger ]; then
    update-alternatives --remove $notiger $bindir/$tiger
  fi
done

#DEBHELPER#

exit 0
